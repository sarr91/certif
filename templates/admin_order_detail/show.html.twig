{% extends 'base.html.twig' %}

{% block title %}OrderDetail{% endblock %}

{% block body %}
    <h1>OrderDetail</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ order_detail.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ order_detail.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_order_detail_index') }}">back to list</a>

    <a href="{{ path('admin_order_detail_edit', {'id': order_detail.id}) }}">edit</a>

    {{ include('admin_order_detail/_delete_form.html.twig') }}
{% endblock %}
